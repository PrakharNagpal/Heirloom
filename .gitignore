# ---------------------------------------------------------------------------
# Private — never push. The repo goes public for the submission.
# ---------------------------------------------------------------------------

# The build plan: strategy, cut rules, competitive read, video script.
# Working document, not a deliverable.
HEIRLOOM.md
CLAUDE.md
AGENTS.md
.claude/

# Anything that carries a real person's recording or transcript.
understand-output.json
public/spot-check.html
public/demo.*
public/*.recording.*
notes/
scratch/

# Credentials. .env.example is the documented template and does get pushed.
.env*
!.env.example
*.pem
*-service-account*.json
gha-creds-*.json

# ---------------------------------------------------------------------------
# Generated
# ---------------------------------------------------------------------------

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# deploy
.vercel

# test output
/coverage
/test-results/
/playwright-report/
/blob-report/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# images — screenshots, gate output, scratch renders. The app's own icons are the
# exception: manifest.json and the layout reference them, so the deploy needs them.
*.png
!public/icon-192.png
!public/icon-512.png
!public/icon-maskable-512.png
!public/apple-touch-icon.png

# The exception to the exception: README screenshots are a deliverable. They are
# `npm run shots` output off the seeded memories, so no real person is in them.
!docs/screenshots/*.png

# editors and OS
.DS_Store
.idea/
.vscode/
*.swp
generate-output.json
